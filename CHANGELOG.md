# Changelog

## 1.0.0 - 2026-06-11

🎆 第一个稳定大版本。

- 每应用横屏 / 竖屏各一套坐标，每个方向独立「使用肩键」开关。
- 旋转跟随：设备旋转自动套用当前方向坐标；该方向未启用则干净释放。
- 坐标越界保护：输入即校验（超界提示并保留原值），套用前再合法化，杜绝屏外注入（根治 bug2）。
- 干净释放：离开目标应用用原生 disable 序列，不再用 releaseTgk（实测 releaseTgk 是“全开”而非释放，曾导致出应用肩键残留）。
- 应用选择器：前台 / 活跃应用改取真实多任务列表；历史应用（曾配置过的）折叠展开。
- 新建配置默认竖屏；横屏默认坐标 L(800,600) / R(1700,600)。

## 0.2.1 - 2026-06-07

- 增加正式 release 签名流程，GitHub Actions 使用 release keystore 构建可稳定升级的 APK。
- 签名很重要：Android 用签名证书识别应用身份，后续版本必须继续使用同一个 key 才能覆盖升级。
